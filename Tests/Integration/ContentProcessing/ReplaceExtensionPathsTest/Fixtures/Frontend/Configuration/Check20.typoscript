plugin.tx_coreextended {

    settings {
        cdn {
            ignoreIfContains = New-Setup-Test
        }
	}
}

// needed because of BE-Mode with test
module.tx_coreextended < plugin.tx_coreextended
